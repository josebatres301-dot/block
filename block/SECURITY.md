# Security

Pair is a private PWA for two users (Jose and Yareli). All data is stored in Firebase Firestore under anonymous auth UIDs. There are no public-facing APIs or user-generated content pipelines. If you discover a security issue — such as Firestore rules that allow unintended reads or writes — please open a private issue or contact the repo owner directly rather than filing a public bug report.
